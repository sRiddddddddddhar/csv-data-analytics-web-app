{
  "frontend/src/utils/csvParser.ts": "Pure browser CSV parser handling quoted fields, BOM, and blank row filtering; returns headers and typed row objects.",
  "frontend/src/components/CsvUpload.tsx": "CSV upload component with corrected `detectColumnTypes(rows, headers)` argument order, correct `{ cleaned, summary }` destructuring from `cleanData`, and correct 5-argument `setDataset` call signature.",
  "frontend/src/utils/pdfExporter.ts": "Comprehensive PDF export utility that now programmatically generates inline SVG charts (bar, line, scatter, pie) from the dataset using `selectChartConfig` and `prepareChartData`, eliminating the \"Chart not available\" placeholder. Falls back to DOM capture if the Charts tab is active, then to the auto-generated SVG, ensuring a real chart always appears in the PDF's Section 03.",
  "frontend/src/components/Sidebar.tsx": "Dark sidebar with all existing navigation, PDF export, and collapse behavior preserved; adds an optional onLogout prop that renders a \"Sign Out\" button at the bottom of the sidebar when provided.",
  "frontend/src/App.tsx": "Main app shell unchanged except for importing useAuth and LoginOverlay — the overlay is rendered as an additive top-level layer when isAuthenticated is false, and the logout function is passed to Sidebar via the new onLogout prop.",
  "frontend/src/hooks/useQueries.ts": "Placeholder module for React Query hooks; no backend queries are needed since all processing is client-side.",
  "frontend/src/components/DataPreviewTable.tsx": "Paginated sortable data table with dark search input, subtle row hover, sort indicators, and pagination controls styled for the dark theme.",
  "frontend/src/utils/insightsGenerator.ts": "Generates data-driven insights covering correlations, outliers, trends, dominant categories, and missing values using all analytics engines.",
  "frontend/src/components/InsightsPanel.tsx": "Insights panel with severity-colored left-border cards (rose/amber/indigo), grouped by category, with hover lift animation and dark theme styling.",
  "frontend/src/utils/statisticsEngine.ts": "Computes descriptive statistics (mean, median, min, max, stdDev, variance, count, nullCount) for numeric columns; exports ColumnStats interface and computeAllStats helper.",
  "frontend/src/utils/columnTypeDetector.ts": "Detects column types (numeric, categorical, date) from CSV rows using pattern matching and sampling; exports ColumnMeta interface and detectColumnTypes function.",
  "frontend/src/utils/dataCleanser.ts": "Cleans raw CSV rows by imputing missing numeric values with column mean, categorical with 'N/A', and tracking pre-imputation missing counts in a CleaningSummary object.",
  "frontend/src/utils/correlationEngine.ts": "Computes Pearson correlation coefficients between numeric column pairs and returns a full correlation matrix.",
  "frontend/src/utils/outlierDetector.ts": "Detects outliers in numeric columns using the IQR method, returning Q1/Q3/IQR/bounds/outlier counts and sample values per column.",
  "frontend/src/utils/regressionEngine.ts": "Performs linear regression and trend detection (rising/falling/flat) for numeric columns against a datetime column.",
  "frontend/src/utils/aiCopilotEngine.ts": "Updated to import `selectChartConfig` and `prepareChartData` (replacing the removed `autoSelectChart`) and uses a `buildChartConfig` helper that calls the new three-argument `prepareChartData` API correctly throughout.",
  "frontend/src/store/dataStore.ts": "Zustand store holding the loaded dataset state (filename, rawData, cleanedData, columns, cleaningSummary) with setDataset and clearDataset actions.",
  "frontend/src/components/KpiDashboard.tsx": "KPI dashboard using the correct `CleaningSummary.missingBeforeImputation` and `imputedCells` fields (not the non-existent `missingCounts`), with dark-themed glow cards and column overview.",
  "frontend/src/components/analytics/StatisticsPanel.tsx": "Descriptive statistics table with sticky column header, alternating row shading, monospace values, and dark panel styling.",
  "frontend/src/components/analytics/CorrelationHeatmap.tsx": "Pearson correlation heatmap with indigo/rose color scale cells, hover scale effect, legend, and dark panel styling.",
  "frontend/src/components/analytics/OutlierSummary.tsx": "IQR outlier table with amber/green count badges, alternating rows, and dark panel styling with summary badge.",
  "frontend/src/components/charts/ChartControls.tsx": "Chart configuration panel with chart type toggle buttons and X/Y axis column selectors; both axis dropdowns now show all available columns regardless of detected type (numeric, categorical, or date).",
  "frontend/src/components/charts/DynamicChart.tsx": "Recharts renderer that correctly uses `prepareChartData` returning a `PreparedChartData` object (accessing `.data`, `.xKey`, `.yKey`) with dark-mode palette, custom tooltip, and proper type casting for `cleanedData`.",
  "frontend/src/components/AICopilotPanel.tsx": "Chat panel using the correct `CopilotResponse.text` field (not `.answer`) and removing the non-existent `.table` property; renders user/AI bubbles with gradient styling and inline charts from the copilot engine.",
  "frontend/src/index.css": "Global dark-first stylesheet with OKLCH color tokens; replaces all `@apply` opacity-modifier classes (like `border-border/50`) with plain CSS `color-mix()` or direct `var()` references to avoid Tailwind v3 `@apply` limitations, while preserving the full dark theme design.",
  "frontend/tailwind.config.js": "Tailwind config with OKLCH CSS variable color tokens, Inter/JetBrains Mono fonts, extended border-radius/shadow/animation scales including glow and card-hover shadows.",
  "frontend/src/utils/chartSelector.ts": "Rewrote chart selector with explicit `selectChartConfig` (auto-detects best chart type and columns) and `prepareChartData` (builds Recharts-ready data arrays from real CSV rows for bar/line/scatter/pie/histogram); no hardcoded demo data.",
  "frontend/src/components/PredictiveModelPanel.tsx": "Regression panel using the actual exported `linearRegression` and `detectTrends` from `regressionEngine` (no `runRegression`/`RegressionResult` exports), with gradient run button and result metrics.",
  "frontend/index.html": "HTML entry point with the browser tab title updated to \"InsightIQ\".",
  "frontend/src/hooks/useAuth.ts": "Custom React hook that manages authentication state using localStorage, providing isAuthenticated boolean, a login function that writes a session token, and a logout function that removes it.",
  "frontend/src/components/LoginOverlay.tsx": "Full-screen fixed overlay (z-index 9999) rendered when the user is not authenticated, containing a centered card with email/password inputs, show/hide password toggle, validation error display, and a loading spinner on submit."
}